// Rcon automatisch laden


// Hier eure ServerIP rein, aber ohne den Port e.g.84.254.76.100
rcon_address   "85.10.208.100"

// Hier euren ServerPort rein e.g. 27015
rcon_port "27022"

// Hier euer RCON-Password rein e.g. linemax
rcon_password  "Diana"

// Mapchange
// DE_maps

// de_nuke
alias nuke "rcon changelevel de_nuke"

// dust
alias dust "rcon changelevel de_dust"

// dust2
alias dust2 "rcon changelevel de_dust2"

// train
alias train "rcon changelevel de_train"

// inferno
alias inferno "rcon changelevel de_inferno"

// cbble
alias cbble "rcon changelevel de_cbble"

// azze
alias aztec "rcon changelevel de_aztec"

// chateau
alias chateau "rcon changelevel de_chateau"

// prodigy
alias prodigy "rcon changelevel de_prodigy"

// piranesi
alias piranesi "rcon changelevel de_piranesi"

// de_port
alias port "rcon changelevel de_port"

// tides
alias tides "rcon changelevel de_tides"

// CS_maps

// assault
alias assault "rcon changelevel cs_assault"

// compound
alias compound "rcon changelevel cs_compound"

// havana
alias havana "rcon changelevel cs_havana"

// italy
alias italy "rcon changelevel cs_italy"

// militia
alias militia "rcon changelevel cs_militia"

// office
alias office "rcon changelevel cs_office"

// bots kicken/adden

alias bot_add_t "rcon bot_add_t"

alias bot_add_ct "rcon bot_add_ct"

alias bot_kick "rcon bot_kick"

// Fadetoblack aus/an

// Fadetoblack aus
alias ftb 0 "rcon mp_fadetoblack 0;wait10; rcon say Now fadetoblack is deactivated."

// Fadetoblack an
alias ftb 1 "rcon mp_fadetoblack 1;wait10; rcon say Now fadetoblack is enabled."

// 1 Restart
alias rr "rcon mp_restartgame 1"

// 3 Restarts + Warmup
alias warmup "rcon exec warmup"

// ESl1on1 + Kniferound
alias k_1 "rcon exec esl1on1; wait10; wait10; rcon exec knife"

// ESl2on2 + Kniferound
alias k_2 "rcon exec esl2on2; wait10; wait10; rcon exec knife"

// ESl3on3 + Kniferound
alias k_3 "rcon exec esl3on3; wait10; wait10; rcon exec knife"

// ESl5on5 + Kniferound
alias k_5 "rcon exec esl5on5; wait10; wait10; rcon exec knife"


// ESL1on1 + Live
alias l_11 "rcon exec esl1on1; wait10; wait10; rcon exec live"

// ESL2on2 + Live
alias l_2 "rcon exec esl2on2; wait10; wait10; rcon exec live"

// ESL3on3 + Live
alias l_3 "rcon exec esl3on3; wait10; wait10; rcon exec live"

// ESL5on5 + Live
alias l_5 "rcon exec esl5on5; wait10; wait10; rcon exec live"


// ESL1on1 + Training
alias train_1on1 "rcon exec esl1on1; wait10; wait10; rcon exec train"

// ESL2on2 + Training
alias train_2on2 "rcon exec esl2on2; wait10; wait10; rcon exec train"

// ESL3on3 + Training
alias train_3on3 "rcon exec esl3on3; wait10; wait10; rcon exec train"

// ESL5on5 + Training
alias train_5on5 "rcon exec esl5on5; wait10; wait10; rcon exec train"

// ESLfun1 + Training
alias train_f2 "rcon exec eslfml; wait10; wait10; rcon exec train"

// ESLfun2 + Training
alias train_f1 "rcon exec eslfml1; wait10; wait10; rcon exec train"


// Settings

// Verzögerung nicht ändern!
alias wait10 "wait; wait; wait; wait; wait; wait; wait; wait; wait; wait"

// Info das Script geladen ist
echo "Rconconfig von www.Linemax.de erfolgreich geladen."